# StoryBranch — Codex 执行约束（AGENTS.md）

> 由原始 `CODEX.md` 直接拆分。除文件标题与本说明外，以下正文保持原文，不做删减、改写或精简。

## 1. Codex 工作方式

Codex 在执行任务时必须遵守：

1. 先理解需求，再编码；
2. 不主动扩大需求；
3. 每次只处理一个明确任务；
4. 修改前检查现有代码；
5. 优先修改已有实现，而不是重写整个项目；
6. 避免无意义重构；
7. 避免过度抽象；
8. 不为未来可能的需求提前设计复杂架构；
9. 新增依赖前必须确认是否真的必要；
10. 优先使用现有依赖和原生能力；
11. 每完成一个阶段都要运行必要检查；
12. 不允许在已有功能正常时为了“代码更漂亮”做大规模重构；
13. 如果需求存在不确定性，优先采用最简单、最稳定、最容易解释的方案；
14. 如果某功能可能明显扩大开发时间，应停止并指出风险；
15. 出现 Bug 时优先修复根因，不使用大量临时 hack；
16. 不要一次性生成整个项目后再统一排错。

---

## 2. Codex 每个任务完成后的输出要求

完成一个独立任务后，需要说明：

## 完成内容

本次实现了什么。

## 修改文件

列出修改或新增的主要文件。

例如：

```text
src/components/BranchCard.vue
src/types/story.ts
src/composables/useStory.ts
```

## 验证

说明：

- 执行了什么检查；
- 是否通过；
- 用户应该如何手动验证。

## 下一步

只说明下一项最直接的开发任务。

不要一次规划十几个后续优化。

---

## 3. 新增依赖规则

新增任何 npm 依赖前先判断：

> 是否可以用现有依赖或几十行代码解决？

如果可以，则禁止新增依赖。

尤其不要为了以下简单需求增加重量级依赖：

- Story Tree；
- ID 生成；
- localStorage；
- Loading；
- 请求状态；
- 简单动画；
- JSON 解析；
- 基础表单；
- 简单状态管理。

---

## 4. 代码设计原则

代码需要：

- 易读；
- 类型明确；
- 功能职责清晰；
- 不过度抽象；
- 适合面试解释。

不需要：

- 企业级分层；
- 大量 Factory；
- Repository Pattern；
- Event Bus；
- CQRS；
- DDD；
- 复杂依赖注入；
- 大量 Interface 套 Interface。

这是一个 MVP，而不是大型企业后台。

---

## 5. API 错误处理

Node API 至少处理：

- 参数缺失；
- LLM 请求失败；
- LLM 超时；
- LLM 返回空内容；
- JSON 解析失败；
- JSON 字段缺失。

对前端返回统一、可理解的错误。

不要直接把：

- API Key；
- 完整内部错误栈；
- 第三方服务敏感信息；

返回浏览器。

---

## 6. Codex 首次启动 Prompt

第一次让 Codex 接手项目时，发送：

```text
请完整阅读项目根目录下的 CODEX.md。

现在先不要编写任何代码。

请根据 CODEX.md 分析当前项目，并输出：

1. 你对产品核心目标的理解；
2. MVP 核心用户路径；
3. 当前项目需要实现的模块；
4. 推荐的数据结构；
5. 前后端数据流；
6. 两个 LLM 任务分别负责什么；
7. Mock 阶段如何跑通；
8. 真实 LLM 接入顺序；
9. 当前最可能出现的技术风险；
10. 哪些功能明确不应该做。

要求：

- 严格遵守 CODEX.md；
- 不主动扩展功能；
- 不增加不必要依赖；
- 不进行过度设计；
- 项目必须以 3～5 小时完成为前提；
- 现在只做分析，不修改任何文件。
```

---

## 7. Codex 正式开发 Prompt

确认规划没有跑偏后，发送：

```text
按照 CODEX.md 开始开发。

先执行 P0，并严格按照 CODEX.md 中的开发顺序推进。

要求：

1. 一次只完成一个独立任务；
2. 优先跑通 Mock 核心流程；
3. Mock 核心流程验证通过后再接真实 LLM；
4. 不主动扩大需求；
5. 不增加不必要依赖；
6. 不进行无意义重构；
7. 每完成一个任务后都要：
   - 说明完成内容；
   - 列出修改文件；
   - 执行必要检查；
   - 告诉我如何手动验证；
   - 再进入下一项。
8. 如果发现某项实现会明显增加复杂度或开发时间，先停止并说明，不要自行扩大范围。

现在从第一项任务开始。
```

---

## 8. Codex 跑偏时的纠正 Prompt

如果 Codex 开始增加数据库、Agent、复杂架构等，发送：

```text
停止当前扩展。

重新阅读 CODEX.md。

你正在实现超出 MVP 范围的内容。

请：

1. 列出当前超出 CODEX.md 范围的设计或代码；
2. 删除或回退这些不必要实现；
3. 恢复到最小可用方案；
4. 继续当前 P0 核心用户路径。

不要增加新的功能或技术栈。
```

---

## 9. Bug 修复 Prompt

遇到 Bug 时：

```text
先不要新增功能。

请定位当前问题的根因。

要求：

1. 先阅读相关代码；
2. 说明问题出现在哪一层；
3. 给出最小修改方案；
4. 不要重写无关模块；
5. 不要为了修复这个问题引入新的架构或依赖；
6. 修改后执行必要验证；
7. 告诉我如何复现和验证问题已经解决。
```

---

## 10. 最终验收 Prompt

功能基本完成以后，发送：

```text
现在停止新增功能。

请根据 CODEX.md 的「验收标准」逐项检查当前项目。

输出：

1. 已通过的验收项；
2. 未通过的验收项；
3. 当前存在的 Bug；
4. Console / TypeScript / Build 是否存在错误；
5. API Key 是否安全；
6. 核心流程是否能够完整跑通；
7. 在不扩大范围的前提下，提交前还必须修复什么。

只处理影响提交和演示的问题。

不要提出新的产品功能。
```

---

## 11. 最终原则

如果 Codex 在任何时候不确定应该：

- 做更多；
- 还是做更少；

默认选择：

> **做更少，但把核心路径做完整。**

如果不确定应该：

- 使用复杂技术；
- 还是简单实现；

默认选择：

> **简单实现。**

如果不确定应该：

- 增加新功能；
- 还是修复现有体验；

默认选择：

> **修复现有体验。**

如果 P0、P1 已经全部完成：

> **停止开发，进入部署、README 和演示准备阶段。**
